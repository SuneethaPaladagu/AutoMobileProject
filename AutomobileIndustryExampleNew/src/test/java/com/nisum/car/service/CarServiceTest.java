package com.nisum.car.service;

import static org.mockito.Mockito.doNothing;
import static org.mockito.Mockito.when;

import java.util.ArrayList;
import java.util.Date;
import java.util.List;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.Spy;
import org.mockito.runners.MockitoJUnitRunner;

import com.nisum.car.Car;
import com.nisum.service.CarService;;

@RunWith(MockitoJUnitRunner.class)
public class CarServiceTest {
	
/*	@InjectMocks
	CarService CarService;
	
	@Mock
	Car car;
	@Spy
	FoodMapper foodMapper=new FoodMapper();
	
	@Test
	public void saveCarTest(){
		Car car=new Car();
		car.setMake("New");
		car.setModel("M1");
		Food food=foodMapper.carToFood(car);
		when((car).createFood(foodMapper.carToFood(car))).thenReturn(food);
		CarService.save(car);
		
	}
	@Test
	public void deleteFoodTest(){
	String id="1";
	doNothing().when(car).delete(Mockito.anyString());
		CarService.deleteFood(id);
		
	}
	@Test
	public void getAllFoodTest(){
		List<Food> li_food=new ArrayList<Food>();
		Mockito.when((car).findAllFood()).thenReturn(li_food);
		CarService.getAllFood();
	
	}
	@Test
	public void getFoodbetweenDateTest(){
	Date startDate=new Date();
	Date endDate=new Date();
	List<Food> li_food=new ArrayList<Food>();
	Mockito.when((car).getFoodBetweenDate(new Date(), new Date())).thenReturn(li_food);
	CarService.getFoodBetweenDate(startDate, endDate);
	
		
	}
	@Test
	public void getFoodByIdTest(){
		String id="11";
		Food food=new Food();
		Mockito.when((car).getOne("1")).thenReturn(food);
		CarService.getFoodById(id);
		
		
	}*/
	

}
